
export const metadata = {
    title: "CVConnect | Home Owner - Create Transaction",
    description: "Integrated solutions for record management.",
  };

export default function Layout({ children }) {
    return (
        <>
            {children}
        </>
          
    );
}