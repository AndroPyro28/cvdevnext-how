export const metadata = {
    title: "CVConnect | Home Owner - Billing statements",
    description: "Integrated solutions for record management.",
  };
  
export default function Layout({ children }) {
    return (
       <>
       {children}
       </>
           
    );
}